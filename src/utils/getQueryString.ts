// import { Obj } from '@model/index';

// /* TODO: type check */

// export default function parseQuery(asPath: string = '/mbti?id=1') {
//   const queryStr = asPath.split('?')[1];
//   if (queryStr) {
//     return queryStr.split('&').reduce((o: {}, curr) => {
//       const [k, v] = curr.split('=');
//       o[k] = v;

//       return o;
//     }, {});
//   }

//   return null;
// }
